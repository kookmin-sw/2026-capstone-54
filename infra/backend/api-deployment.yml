apiVersion: apps/v1
kind: Deployment
metadata:
  name: mefit-production-api
  namespace: mefit-backend-production
  labels:
    app: mefit-production-api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: mefit-production-api
  template:
    metadata:
      labels:
        app: mefit-production-api
    spec:
      # replicas:2 + 각 limit 1.2 vCPU/1229Mi → t3.small(2 vCPU/2GiB) 에 한 replica 라도 가면
      # 노드 거의 전체를 점유, OOM/Pending 위험. 반드시 m5.large 에 스케줄.
      nodeSelector:
        nodepool: heavy
      imagePullSecrets:
      - name: mefit-production-dockerhub
      containers:
      - name: django
        image: teammefit/mefit-backend:production
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: mefit-production-env
        - secretRef:
            name: mefit-production-secret
        env:
        - name: DJANGO_APPLICATION_NAME
          value: "mefit-api"
        command: [ "uvicorn" ]
        args:
        - "config.asgi:application"
        - "--host"
        - "0.0.0.0"
        - "--port"
        - "8000"
        - "--workers"
        - "2"
        readinessProbe:
          httpGet:
            path: /api/v1/health-check/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /api/v1/health-check/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: 150m
            memory: 640Mi
          limits:
            cpu: "1.2"
            memory: 1229Mi
